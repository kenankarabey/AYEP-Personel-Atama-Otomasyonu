import React from 'react';
import { useAlert } from './AlertContext';

const Alert = () => {
  // Bu bileşen AlertProvider'ın render ettiği alerti göstermek için kullanılabilir.
  // Şu an için AlertProvider zaten alerti render ediyor, burada ekstra bir şey gerekmez.
  // İleride özel bir yerde göstermek isterseniz bu bileşeni kullanabilirsiniz.
  return null;
};

export default Alert; 